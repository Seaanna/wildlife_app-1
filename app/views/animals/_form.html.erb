<div class="row">
  <div class="col-xs-6 col-xs-offset-3 text-center">
    <%= form_for(@animal) do |f| %>
      <% if @animal.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@animal.errors.count, "error") %> prohibited this animal from being saved:</h2>

          <ul>
            <% @animal.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="field">
        <%= f.label :common_name, class: 'bigger' %><br>
        <%= f.text_field :common_name, class: 'form-control' %>
      </div>
      <div class="space-14"></div>
      <div class="field">
        <%= f.label :latin_name, class: 'bigger'  %><br>
        <%= f.text_field :latin_name, class: 'form-control' %>
      </div>
      <div class="space-14"></div>
      <div class="field">
        <%= f.label :kingdom, class: 'bigger'  %><br>
        <%= f.text_field :kingdom, class: 'form-control' %>
      </div>
      <div class="space-24"></div>

      <div class="actions">
        <%= f.submit class: "btn btn-success" %>
      </div>
    <% end %>
  </div>
</div>

